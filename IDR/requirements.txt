# IDR analysis pipeline dependencies
#
# MobiDB-lite: install from GitHub (includes all Python predictors)
git+https://github.com/BioComputingUP/MobiDB-lite.git
joblib>=1.3.0
numba>=0.59.0
scikit-learn>=1.4.0
numpy>=1.26.4
scipy>=1.13.0
#
# AIUPred: NOT pip-installable; clone manually and set AIUPRED_DIR.
# Clone: git clone https://github.com/doszilab/AIUPred.git
# Then install its Python deps (use torch>=2.6.0 to avoid known CVEs):
torch>=2.6.0
#
# General
pandas>=2.0.0
requests>=2.28.0
